import { CallHandler, ExecutionContext, NestInterceptor } from "@nestjs/common";
import { z } from "zod";
import { Observable } from "rxjs";
import { BorderConfiguration } from "./types";
export declare class BorderPatrolInterceptor<TBody extends z.ZodSchema | undefined, TQuery extends Record<string, z.ZodSchema | undefined> | undefined, TParams extends Record<string, z.ZodSchema | undefined> | undefined, TResponse extends z.ZodSchema | undefined> implements NestInterceptor {
    private readonly config;
    constructor(config: BorderConfiguration<TBody, TQuery, TParams, TResponse>);
    intercept(context: ExecutionContext, next: CallHandler): Observable<any>;
}

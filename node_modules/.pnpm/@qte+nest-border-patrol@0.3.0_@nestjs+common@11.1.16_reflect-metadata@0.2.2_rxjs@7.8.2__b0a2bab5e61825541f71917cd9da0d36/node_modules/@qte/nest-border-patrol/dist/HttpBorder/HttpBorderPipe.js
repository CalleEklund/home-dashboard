"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "HttpBorderPipe", {
    enumerable: true,
    get: function() {
        return HttpBorderPipe;
    }
});
const _common = require("@nestjs/common");
const _HttpBorder = require("./HttpBorder");
const _errors = require("./errors/index");
function _ts_decorate(decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
}
function _ts_metadata(k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
}
let HttpBorderPipe = class HttpBorderPipe {
    border;
    constructor(border){
        this.border = border;
    }
    transform(value, metadata) {
        switch(metadata.type){
            case 'body':
                {
                    if (metadata.data) {
                        throw new _common.NotImplementedException();
                    }
                    const schema = this.border.getBodySchema();
                    if (!schema) {
                        return undefined;
                    }
                    const result = schema.safeParse(value);
                    if (result.success) {
                        return result.data;
                    }
                    throw new _errors.InvalidBodyError();
                }
            case 'custom':
                {
                    return value;
                }
            case 'param':
                {
                    if (metadata.data) {
                        throw new _common.NotImplementedException('Pipe does not support subpaths');
                    }
                    const schemas = this.border.getParamsSchema();
                    const parsedValue = {};
                    Object.entries(schemas).filter((entry)=>{
                        if (!entry[1]) {
                            return false;
                        }
                        return true;
                    }).forEach(([key, schema])=>{
                        const result = schema.safeParse(value[key]);
                        if (result.success) {
                            parsedValue[key] = result.data;
                            return;
                        }
                        throw new _errors.InvalidPathParameterError();
                    });
                    return parsedValue;
                }
            case 'query':
                {
                    if (metadata.data) {
                        throw new _common.NotImplementedException('Pipe does not support subpaths');
                    }
                    const schemas = this.border.getQuerySchema();
                    const parsedValue = {};
                    Object.entries(schemas).filter((entry)=>{
                        if (!entry[1]) {
                            return false;
                        }
                        return true;
                    }).forEach(([key, schema])=>{
                        const result = schema.safeParse(value[key]);
                        if (result.success) {
                            parsedValue[key] = result.data;
                            return;
                        }
                        throw new _errors.InvalidQueryParameterError();
                    });
                    return parsedValue;
                }
        }
    }
};
HttpBorderPipe = _ts_decorate([
    (0, _common.Injectable)(),
    _ts_metadata("design:type", Function),
    _ts_metadata("design:paramtypes", [
        typeof _HttpBorder.HttpBorder === "undefined" ? Object : _HttpBorder.HttpBorder
    ])
], HttpBorderPipe);

//# sourceMappingURL=HttpBorderPipe.js.map
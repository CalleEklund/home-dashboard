import { HttpStatus } from '@nestjs/common';
export declare class HttpBorderResponse<TStatusCode extends HttpStatus, TBody> {
    readonly statusCode: TStatusCode;
    readonly body: TBody;
    constructor(options: {
        statusCode: TStatusCode;
        body: TBody;
    });
}

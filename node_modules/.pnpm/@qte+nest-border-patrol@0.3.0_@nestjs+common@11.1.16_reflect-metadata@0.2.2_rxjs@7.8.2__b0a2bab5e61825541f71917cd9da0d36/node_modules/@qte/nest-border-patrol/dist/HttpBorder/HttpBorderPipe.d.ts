import { ArgumentMetadata, HttpStatus, PipeTransform } from '@nestjs/common';
import { HttpBorder } from './HttpBorder';
import { z } from 'zod';
export declare class HttpBorderPipe<TBody extends z.ZodSchema | undefined, TParams extends Record<string, z.ZodSchema | undefined>, TQuery extends Record<string, z.ZodSchema | undefined>, TResponses extends {
    [K in HttpStatus]?: z.ZodSchema;
}> implements PipeTransform {
    private border;
    constructor(border: HttpBorder<TBody, TParams, TQuery, TResponses>);
    transform(value: any, metadata: ArgumentMetadata): any;
}

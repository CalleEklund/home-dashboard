import { HttpStatus } from '@nestjs/common';
import { z } from 'zod';
import { HttpBorderResponse } from './HttpBorderResponse';
export declare class HttpBorder<TBody extends z.ZodSchema | undefined, TParams extends Record<string, z.ZodSchema | undefined>, TQuery extends Record<string, z.ZodSchema | undefined>, TResponses extends {
    [K in HttpStatus]?: z.ZodSchema;
}> {
    private readonly bodySchema;
    private readonly paramsSchema;
    private readonly querySchema;
    private readonly responsesSchema;
    constructor(options: {
        requestBody?: TBody;
        parameters?: {
            path?: TParams;
            query?: TQuery;
        };
        responses: TResponses;
    });
    createResponse<TCode extends HttpStatus>(statusCode: TCode, body: TCode extends keyof TResponses ? TResponses[TCode] extends z.ZodSchema ? z.infer<TResponses[TCode]> : never : never): HttpBorderResponse<TCode, TCode extends keyof TResponses ? TResponses[TCode] extends z.ZodType<any, z.ZodTypeDef, any> ? z.TypeOf<TResponses[TCode]> : never : never>;
    getBodySchema(): TBody;
    getParamsSchema(): TParams;
    getQuerySchema(): TQuery;
    getResponseSchema<TCode extends HttpStatus>(code: TCode): z.ZodSchema | undefined;
    getResponsesSchema(): TResponses;
}

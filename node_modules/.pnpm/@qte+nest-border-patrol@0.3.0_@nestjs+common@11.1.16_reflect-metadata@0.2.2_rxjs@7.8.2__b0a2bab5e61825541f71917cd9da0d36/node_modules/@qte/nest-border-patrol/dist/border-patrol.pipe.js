"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "BorderPatrolPipe", {
    enumerable: true,
    get: function() {
        return BorderPatrolPipe;
    }
});
const _common = require("@nestjs/common");
const _zod = require("zod");
const _types = require("./types/index");
const _borderpatrolexception = require("./border-patrol.exception");
function _ts_decorate(decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
}
function _ts_metadata(k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
}
let QueryOrParamsException = class QueryOrParamsException extends Error {
    zodError;
    key;
    constructor(zodError, key){
        super();
        this.zodError = zodError;
        this.key = key;
    }
};
let BorderPatrolPipe = class BorderPatrolPipe {
    config;
    constructor(config){
        this.config = config;
    }
    transform(value, metadata) {
        try {
            switch(metadata.type){
                case "body":
                    if (metadata.data) {
                        throw new _common.NotImplementedException();
                    }
                    return this.config.body ? this.config.body.parse(value) : undefined;
                case "query":
                    {
                        if (!this.config.query) {
                            return undefined;
                        }
                        if (metadata.data) {
                            const schema = this.config.query[metadata.data];
                            if (!schema) {
                                return undefined;
                            }
                            try {
                                return schema.parse(value);
                            } catch (e) {
                                if (e instanceof _zod.ZodError) {
                                    throw new QueryOrParamsException(e, metadata.data);
                                }
                                throw e;
                            }
                        }
                        const parsedValue = {};
                        Object.entries(this.config.query).filter((entry)=>{
                            if (!entry[1]) {
                                return false;
                            }
                            return true;
                        }).forEach(([key, schema])=>{
                            try {
                                parsedValue[key] = schema.parse(value[key]);
                            } catch (e) {
                                if (e instanceof _zod.ZodError) {
                                    throw new QueryOrParamsException(e, key);
                                }
                                throw e;
                            }
                        });
                        return parsedValue;
                    }
                case "param":
                    {
                        if (!this.config.params) {
                            return undefined;
                        }
                        if (metadata.data) {
                            const schema = this.config.params[metadata.data];
                            if (!schema) {
                                return undefined;
                            }
                            try {
                                return schema.parse(value);
                            } catch (e) {
                                if (e instanceof _zod.ZodError) {
                                    throw new QueryOrParamsException(e, metadata.data);
                                }
                                throw e;
                            }
                        }
                        const parsedValue = {};
                        Object.entries(this.config.params).filter((entry)=>{
                            if (!entry[1]) {
                                return false;
                            }
                            return true;
                        }).forEach(([key, schema])=>{
                            try {
                                parsedValue[key] = schema.parse(value[key]);
                            } catch (e) {
                                if (e instanceof _zod.ZodError) {
                                    throw new QueryOrParamsException(e, key);
                                }
                                throw e;
                            }
                        });
                        return parsedValue;
                    }
                case "custom":
                    return value;
                default:
                    throw new _common.NotImplementedException("Unexpected metadata.type");
            }
        } catch (err) {
            let where;
            if (metadata.type === "custom") {
                throw err;
            }
            if (metadata.type === "param") {
                where = "params";
            } else {
                where = metadata.type;
            }
            if (err instanceof _zod.ZodError) {
                throw new _borderpatrolexception.BorderPatrolException(where, err);
            }
            if (err instanceof QueryOrParamsException) {
                throw new _borderpatrolexception.BorderPatrolException(where, err.zodError, err.key);
            }
            throw err;
        }
    }
};
BorderPatrolPipe = _ts_decorate([
    (0, _common.Injectable)(),
    _ts_metadata("design:type", Function),
    _ts_metadata("design:paramtypes", [
        typeof _types.BorderConfiguration === "undefined" ? Object : _types.BorderConfiguration
    ])
], BorderPatrolPipe);

//# sourceMappingURL=border-patrol.pipe.js.map
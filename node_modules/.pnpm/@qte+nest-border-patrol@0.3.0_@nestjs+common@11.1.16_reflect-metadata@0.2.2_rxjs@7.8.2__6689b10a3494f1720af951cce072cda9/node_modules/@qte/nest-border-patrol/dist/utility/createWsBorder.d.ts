import { z } from "zod";
import { BorderConfiguration } from "../types/BorderConfiguration";
export declare const createWsBorder: <TBody extends z.ZodType<any, z.ZodTypeDef, any>>(schema: TBody) => BorderConfiguration<TBody, undefined, undefined, undefined>;

"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "HttpBorderInterceptor", {
    enumerable: true,
    get: function() {
        return HttpBorderInterceptor;
    }
});
const _common = require("@nestjs/common");
const _rxjs = require("rxjs");
const _HttpBorder = require("./HttpBorder");
const _HttpBorderResponse = require("./HttpBorderResponse");
function _ts_decorate(decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
}
function _ts_metadata(k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
}
let HttpBorderInterceptor = class HttpBorderInterceptor {
    border;
    constructor(border){
        this.border = border;
    }
    intercept(context, next) {
        const ctx = context.switchToHttp();
        const response = ctx.getResponse();
        const request = ctx.getRequest();
        const statusCodeFromNest = response.statusCode;
        // response.st
        return next.handle().pipe((0, _rxjs.map)((data)=>{
            let body;
            let statusCode;
            if (data instanceof _HttpBorderResponse.HttpBorderResponse) {
                statusCode = data.statusCode;
                body = data.body;
            } else {
                statusCode = statusCodeFromNest;
                body = data;
            }
            response.statusCode = statusCode;
            const responseSchema = this.border.getResponseSchema(statusCode);
            if (!responseSchema) {
                throw new Error(`No response schema defined for status code "${statusCodeFromNest}" path: "${request.path}"`);
            }
            const result = responseSchema.safeParse(body);
            if (result.success) {
                return result.data;
            }
            throw result.error;
        // const responseSchema =
        // if (!this.border.getResponseSchema()) {
        //   return data;
        // }
        // // return this.config.response.parse(data);
        // let parsedResponse: any;
        // try {
        //   parsedResponse = this.config.response.parse(data);
        // } catch (err) {
        //   if (err instanceof ZodError) {
        //     throw new BorderPatrolException(
        //       'response',
        //       err
        //       // { error: "Could not validate response" },
        //       // HttpStatus.INTERNAL_SERVER_ERROR,
        //       // { cause: err instanceof Error ? err : undefined }
        //     );
        //   }
        //   throw err;
        // }
        // return parsedResponse;
        }));
    }
};
HttpBorderInterceptor = _ts_decorate([
    (0, _common.Injectable)(),
    _ts_metadata("design:type", Function),
    _ts_metadata("design:paramtypes", [
        typeof _HttpBorder.HttpBorder === "undefined" ? Object : _HttpBorder.HttpBorder
    ])
], HttpBorderInterceptor);

//# sourceMappingURL=HttpBorderInterceptor.js.map
import { ArgumentMetadata, PipeTransform } from "@nestjs/common";
import { z } from "zod";
import { BorderConfiguration } from "./types";
export declare class BorderPatrolPipe<TBody extends z.ZodSchema | undefined, TQuery extends Record<string, z.ZodSchema | undefined> | undefined, TParams extends Record<string, z.ZodSchema | undefined> | undefined, TResponse extends z.ZodSchema | undefined> implements PipeTransform {
    private readonly config;
    constructor(config: BorderConfiguration<TBody, TQuery, TParams, TResponse>);
    transform(value: any, metadata: ArgumentMetadata): any;
}

"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "BorderPatrolInterceptor", {
    enumerable: true,
    get: function() {
        return BorderPatrolInterceptor;
    }
});
const _common = require("@nestjs/common");
const _zod = require("zod");
const _rxjs = require("rxjs");
const _types = require("./types/index");
const _borderpatrolexception = require("./border-patrol.exception");
function _ts_decorate(decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
}
function _ts_metadata(k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
}
let BorderPatrolInterceptor = class BorderPatrolInterceptor {
    config;
    constructor(config){
        this.config = config;
    }
    intercept(context, next) {
        return next.handle().pipe((0, _rxjs.map)((data)=>{
            if (!this.config.response) {
                return data;
            }
            // return this.config.response.parse(data);
            let parsedResponse;
            try {
                parsedResponse = this.config.response.parse(data);
            } catch (err) {
                if (err instanceof _zod.ZodError) {
                    throw new _borderpatrolexception.BorderPatrolException("response", err);
                }
                throw err;
            }
            return parsedResponse;
        }));
    }
};
BorderPatrolInterceptor = _ts_decorate([
    (0, _common.Injectable)(),
    _ts_metadata("design:type", Function),
    _ts_metadata("design:paramtypes", [
        typeof _types.BorderConfiguration === "undefined" ? Object : _types.BorderConfiguration
    ])
], BorderPatrolInterceptor);

//# sourceMappingURL=border-patrol.interceptor.js.map
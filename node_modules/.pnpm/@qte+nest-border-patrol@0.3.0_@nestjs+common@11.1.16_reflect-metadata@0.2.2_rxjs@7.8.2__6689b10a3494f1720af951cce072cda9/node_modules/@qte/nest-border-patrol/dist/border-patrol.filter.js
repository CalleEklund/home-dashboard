"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "BorderPatrolExceptionFilter", {
    enumerable: true,
    get: function() {
        return BorderPatrolExceptionFilter;
    }
});
const _common = require("@nestjs/common");
const _borderpatrolexception = require("./border-patrol.exception");
function _ts_decorate(decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
}
let BorderPatrolExceptionFilter = class BorderPatrolExceptionFilter {
    catch(exception, host) {
        const ctx = host.switchToHttp();
        const response = ctx.getResponse();
        const request = ctx.getRequest();
        if (exception.where === "response") {
            response.status(_common.HttpStatus.INTERNAL_SERVER_ERROR).json({
                status: _common.HttpStatus.INTERNAL_SERVER_ERROR,
                timestamp: new Date().toISOString()
            });
            return;
        }
        response.status(_common.HttpStatus.BAD_REQUEST).json({
            message: `Bad request, invalid ${exception.where}`,
            status: _common.HttpStatus.BAD_REQUEST,
            errors: exception.key ? {
                [exception.key]: exception.zodError.format()
            } : exception.zodError.format(),
            timestamp: new Date().toISOString()
        });
    }
};
BorderPatrolExceptionFilter = _ts_decorate([
    (0, _common.Catch)(_borderpatrolexception.BorderPatrolException)
], BorderPatrolExceptionFilter);

//# sourceMappingURL=border-patrol.filter.js.map